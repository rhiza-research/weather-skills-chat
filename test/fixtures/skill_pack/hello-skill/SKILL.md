---
name: hello-skill
description: Tiny fixture skill that echoes argv and writes a file. Use for skill-install smoke tests.
metadata:
  version: "0.0.1"
---

# hello-skill

## Usage

```
uv run --script ${CLAUDE_SKILL_DIR}/scripts/hello.py --message hi --output out.txt
```
